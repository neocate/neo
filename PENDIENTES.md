# Pendientes

- **Tarea programada de DSM (Programador de tareas)**: confirmado
  2026-08-07 que la opción EXISTE en el DS423 (Synology DSM) — no se
  verificó ni se configuró todavía, solo se comprobó que está disponible.
  Queda pendiente armarla (tarea recurrente, con guardia `pgrep` para no
  duplicar el proceso si ya está corriendo) tanto para `grabador_libro.py`
  como para `fjsl.py` cuando este último exista - se hace en el mismo
  momento, no antes.
